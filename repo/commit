# ---------- Base image ----------
FROM php:8.2-cli

# (Opsional) install ekstensi tambahan
# RUN docker-php-ext-install pdo pdo_mysql

# ---------- Workdir ----------
WORKDIR /app

# ---------- Copy project ----------
COPY . .

# ---------- Jalankan PHP built‑in web server ----------
CMD ["sh", "-c", "php -S 0.0.0.0:80 -t ."]

# WAJIB! Agar Railway bisa expose web port
EXPOSE 80
